---
layout: layout.njk
title: Blog
---

<ul>
    {%- for post in collections.blog | reverse -%}
        <li>
            <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
            <p>{{ post.date | formatDate }}</p>
        </li>
    {%- endfor -%}
</ul>